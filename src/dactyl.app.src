{application, dactyl, [
    {description, "String templating library for Erlang"},
    {vsn, git},
    {registered, []},

    {applications, [
        % OTP dependencies
        kernel,
        stdlib
        % External dependencies
    ]},

    {env, []}
]}.
